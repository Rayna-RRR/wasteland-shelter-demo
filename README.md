# 《废土避难所值班长》原型

## 项目名称

《废土避难所值班长》是一个轻量级废土避难所管理原型，英文项目名为 `wasteland-shelter-demo`。

## 项目定位

这是一个面向作品集展示的轻量游戏原型，重点展示一条可运行、可复盘的系统循环，而不是完整商业游戏。

项目使用原生微信小程序作为前端，Flask 作为后端，SQLite 作为本地数据存储。整体目标是用较小的代码规模表达清楚：幸存者招募、值勤派遣、资源变化、状态反馈、条件式商业化触发和日志记录。

## 当前核心循环

当前核心循环为：

```text
招募 -> 派遣 -> 资源变化 -> 状态反馈 -> 商业化触发 -> 日志记录
```

玩家通过招募获得幸存者，再把幸存者派去外出搜集、发电维护、库存炊事或夜间守卫。后端根据任务结算资源变化，同时更新幸存者疲劳和健康。资源或队伍压力达到条件时，会触发救急型的应急补给协议，相关行为会写入日志。

## 当前已实现

### 幸存者招募

- 支持单次招募幸存者。
- 幸存者具有姓名、稀有度、职业和内部性格来源字段。
- 已拥有的同名幸存者再次出现时，会转化为材料补偿。
- 招募结果会生成入队记录，并写入招募日志。

### 值勤派遣

- 支持选择幸存者并派遣到不同值勤任务。
- 当前值勤类型包括外出搜集、发电维护、库存炊事和夜间守卫。
- 每次值勤会结算食物、电力、材料变化。
- 值勤任务有基础运转成本，部分任务会消耗资源。
- 每隔一定次数会触发避难所例行维护成本。

### 幸存者状态管理

- 幸存者动态状态来自 `fatigue` 和 `health`。
- 当前状态会派生为“状态良好”“疲惫”“状态不稳”“重伤”等动态标签。
- 疲劳过高或健康过低会影响值勤收益。
- 高风险任务可能造成健康下降。
- 静态展示已拆成三层：
  - 特质：一个简短的静态性格词。
  - 作风：描述幸存者平时如何处理工作。
  - 档案：一句废土氛围的人物记录。

### 应急补给协议

应急补给协议是条件触发的救急型设计，不是常驻商城。

当前会根据避难所压力判断是否触发补给协议。压力来源包括食物压力、电力压力、电力短缺、材料压力、多项资源压力、队伍状态压力和近期行动效率下滑。

补给协议出现后，会记录为“已触发”。玩家暂时不处理时，会记录为“已暂缓”。玩家购入后，会记录为“已购入”。

购入应急补给协议后，会补充招募券、食物、电力和材料，并恢复部分幸存者状态。该设计用于表达“压力出现时的救急补给”，而不是完整商店、礼包系统或支付闭环。

本地开发环境下已支持“补给测试状态重置”，方便反复验证补给触发、暂缓和购入流程。

### 日志系统

- 招募日志：记录幸存者姓名、稀有度、职业和时间。
- 值勤日志：记录幸存者、值勤类型、结果文本、资源变化和时间。
- 应急补给日志：记录补给协议已触发、已暂缓、已购入、触发原因、资源快照、幸存者数量和时间。

这些日志用于复盘系统循环，让资源压力、玩家操作、状态变化和补给触发之间的关系更清楚。

### 前端 UI 收口

- 已实现微信小程序首页、招募页、值勤页和日志页。
- 幸存者卡片保留动态状态徽章，并将静态信息拆成“特质 / 作风 / 档案”。
- 招募和值勤页面已经围绕当前玩法循环做了基础视觉收口。
- 当前 UI 以可运行、可展示、可调试为目标，没有接入大型美术资源管线。

## 技术栈

- 前端：原生微信小程序。
- 后端：Python 3.9、Flask、flask-cors。
- 数据库：SQLite，使用 Python 标准库 `sqlite3`。
- 本地开发：macOS、微信开发者工具、VS Code。

## 目录结构

```text
wasteland-shelter-demo/
├── backend/
│   ├── app.py
│   ├── init_db.py
│   ├── cleanup_duplicate_survivors.py
│   └── data/
│       └── game.db
├── miniprogram/
│   ├── app.js
│   ├── app.json
│   ├── app.wxss
│   ├── config.js
│   ├── pages/
│   │   ├── home/
│   │   ├── gacha/
│   │   ├── duty/
│   │   └── logs/
│   └── utils/
│       ├── api.js
│       └── format.js
├── AGENTS.md
└── README.md
```

## 本地运行

初始化或修复本地 SQLite 数据库：

```bash
python backend/init_db.py
```

启动 Flask 后端：

```bash
python backend/app.py
```

后端默认运行在：

```text
http://localhost:5001
```

然后在微信开发者工具中打开 `miniprogram/` 目录，并确认 `miniprogram/config.js` 中的接口地址指向本地 Flask 后端。

## 调试说明

常用接口包括：

- `GET /api/status`：检查后端是否运行。
- `GET /api/resources`：查看当前资源。
- `POST /api/gacha`：执行一次幸存者招募。
- `GET /api/survivors`：查看幸存者列表。
- `POST /api/duty`：派遣幸存者执行值勤。
- `POST /api/rest`：让幸存者休整。
- `GET /api/gacha-logs`：查看招募日志。
- `GET /api/duty-logs`：查看值勤日志。
- 应急补给协议状态接口：查看当前是否已触发补给协议。
- 应急补给协议日志接口：查看补给协议已触发、已暂缓和已购入记录。

本地调试应急补给协议时，可以使用“补给测试状态重置”接口清理补给记录，便于重新验证已触发、已暂缓和已购入流程。

## 当前版本适合展示的点

- 用轻量代码完成了一个完整的管理循环。
- 后端结算逻辑清楚，资源、状态和日志能互相解释。
- 幸存者有动态状态和静态人物展示层，便于表达角色反馈。
- 应急补给协议体现了条件式商业化触发，而不是简单常驻入口。
- 微信小程序前端已经能串起招募、派遣、日志查看等核心页面。
- 适合作为“轻量游戏原型 + 系统设计表达 + 作品集项目”展示。

## 后续计划

- 补充更多幸存者和职业差异。
- 扩展更多值勤类型和风险事件。
- 增加更清晰的数值调试面板。
- 优化日志筛选和复盘体验。
- 在不影响核心结算的前提下，后续可选接入轻量中文模型，仅用于特殊事件文本生成。

当前版本没有完成运行时 LLM 接入，也没有完成大型美术资源管线。资源结算、状态变化和应急补给协议判断仍以本地确定性逻辑为准。

## 说明

本项目重点是“轻量游戏原型 + 作品集表达”。它不宣称已经覆盖完整商业游戏所需的战斗、建筑、合成、付费、账号或线上运营系统。

README 中描述的是当前仓库已经实现并可本地运行的内容；后续计划仅代表可能扩展方向。
